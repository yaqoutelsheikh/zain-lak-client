ALWAYS read and follow `AGENTS.md` in this same directory — it is the
single source of truth for how and what to build in this repo. All rules,
the locked stack, the layer law, and the docs routing live there. This
file intentionally holds no rules of its own.
